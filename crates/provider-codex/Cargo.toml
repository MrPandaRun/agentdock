[package]
name = "provider-codex"
version = "0.1.0"
edition = "2021"
description = "Codex adapter stub for AgentDock"

[dependencies]
provider-contract = { path = "../provider-contract" }
serde_json = { workspace = true }
time = { version = "0.3", features = ["parsing"] }
